import pygame
import os

FichList =[ f for f in os.listdir('/home/pi/Desktop/sons_animaux/') if os.path.isfile(os.path.join('/home/pi/Desktop/sons_animaux/',f)) ]

pygame.mixer.init()
pygame.mixer.pre_init(44100, -16, 2, 2048)
pygame.init()

for i in range(0,
pygame.mixer.music.load('/home/pi/Desktop/sons_animaux/'+FichList[i])
pygame.mixer.music.play()
reponse = raw_input("Ecrire la reponse : ")

print reponse
print FichList
